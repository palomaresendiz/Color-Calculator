/**
 * Main runner that initializes and runs the Calculator UI
 */

package color.calculator;

public class CalculatorMain {

    public static void main(String[] args) {
        CalculatorUI uiCal = new CalculatorUI();
        uiCal.init();
    }
}